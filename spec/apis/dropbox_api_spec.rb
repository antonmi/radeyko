require 'spec_helper'

describe 'Test dropbox api' do

  # APP_KEY = Config::ApiCreds.dropbox[:api_key]
  # APP_SECRET = Config::AppConfig.dropbox[:api_secret]
  # flow = DropboxOAuth2FlowNoRedirect.new(APP_KEY, APP_SECRET)
  # authorize_url = flow.start()

  # access_token, user_id = flow.finish(code)

  # client = DropboxClient.new(access_token)
  # h = client.media '/radeyko/Перкалаба/Перкалаба - Сосни.mp3'
  # opts = { head: {'Range' => 'bytes=0-2000' }}
  # EM.run { http = EventMachine::HttpRequest.new(h['url']).get(opts); http.callback{  @data= http.response; EM.stop } }

end